# 🎮 双人合作模式 - 使用指南

## ✅ 功能已实现
你的地牢游戏已经完整集成了 **双人联网合作打怪** 功能!

---

## ⚠️ 当前状态: 需要部署才能使用联机

### 为什么显示 "Firebase未就绪"?

联机功能依赖 **Firebase Realtime Database**,这是 Google 提供的实时数据库服务。

**本地运行时 (file:// 或 localhost)**:
- ❌ 联机功能不可用
- ✅ 单人模式正常工作
- 原因: 没有 Firebase 配置 (`__firebase_config`)

**部署到支持的平台后**:
- ✅ 联机功能完全可用
- ✅ 单人/双人模式都可选择
- 平台会自动注入 Firebase 配置

---

## 🚀 如何启用联机功能?

### 方法 1: 部署到支持的平台 (推荐)

将游戏部署到以下任一平台:

#### **选项 A: GitHub Pages + Firebase Hosting**
1. 将代码推送到 GitHub 仓库
2. 在 [Firebase Console](https://console.firebase.google.com/) 创建项目
3. 启用 **Realtime Database**
4. 设置安全规则:
```json
{
  "rules": {
    "dungeon_rooms": {
      ".read": true,
      ".write": true
    }
  }
}
5. 使用 Firebase CLI 部署: `firebase deploy`

#### **选项 B: Vercel / Netlify**
这些平台支持自定义环境变量,可以配置 Firebase

#### **选项 C: 你的游戏托管平台**
如果当前平台已集成 Firebase,直接部署即可!

---

### 方法 2: 本地测试 (开发者)

如果你想**立即测试联机功能**,可以手动配置 Firebase:

#### 步骤 1: 创建 Firebase 项目
1. 访问 https://console.firebase.google.com/
2. 点击 "Add project"
3. 项目名称: `dungeon-multiplayer`
4. 启用 Realtime Database

#### 步骤 2: 获取配置
进入 **项目设置** → **您的应用** → **</> 网页应用**

复制 `firebaseConfig` 对象:
```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  databaseURL: "https://YOUR_PROJECT-default-rtdb.firebaseio.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

#### 步骤 3: 修改 k.html (仅用于本地测试)
在 `<script type="module">` 标签前添加:

```html
<script>
// 本地测试用 - 删除此注释和下行以禁用
__firebase_config = JSON.stringify({
  apiKey: "你的API_KEY",
  authDomain: "你的项目.firebaseapp.com",
  databaseURL: "https://你的项目-default-rtdb.firebaseio.com",
  projectId: "你的项目ID",
  // ... 其他配置
});
__app_id = 'dungeon-local-test';
</script>
```

⚠️ **注意**: 这只是临时方案,不要将包含 API Key 的代码公开!

---

## 🎯 联机功能特性

### 已实现的功能:
✅ 房间系统 (创建/加入/列表)  
✅ 实时位置同步 (50ms延迟)  
✅ 双人同时战斗  
✅ 怪物状态同步  
✅ HP/伤害同步  
✅ 第二玩家渲染 (蓝色角色)  
✅ HUD 显示队友状态  
✅ 断线处理  

### 操作方式:
- **P1 (房主)**: WASD移动 + 空格攻击
- **P2 (队友)**: 方向键移动 + Enter攻击 *(可自定义)*

---

## 🔧 故障排查

### 问题: 仍然显示 "Firebase未就绪"

**检查清单:**
1. ✅ 是否已部署? (不是 file:// 协议)
2. ✅ Firebase 项目是否创建?
3. ✅ Realtime Database 是否启用?
4. ✅ 安全规则是否允许读写?
5. ✅ 浏览器控制台是否有错误? (F12 → Console)

**调试命令:**
打开浏览器控制台 (F12),输入:
```javascript
console.log('FB Ready:', window._fb.ready);
console.log('DB:', window._multiplayer.db);
console.log('Config:', typeof __firebase_config !== 'undefined');
```

---

## 💡 替代方案: 如果暂时无法部署

### 方案 1: 本地双人 (同一键盘)
可以修改代码让第二玩家用不同按键控制:
```javascript
// P2 控制: IJKL 移动 + U 攻击
```
但这不是真正的"联网",只是本地多人。

### 方案 2: 使用其他实时通信服务
- Socket.io (需要 Node.js 服务器)
- PeerJS (P2P连接,无需服务器)
- WebSocket (需要后端)

---

## 📞 需要帮助?

如果遇到问题,请提供:
1. 浏览器控制台截图 (F12)
2. 部署平台名称
3. Firebase 配置状态
4. 具体错误信息

---

## 🎉 下一步

1. **立即体验**: 点击 "🎮 单人探险" 开始游戏
2. **准备联机**: 按照上述步骤部署到平台
3. **邀请好友**: 分享房间号,一起打怪!

**祝游戏愉快!** ⚔️👥🎮
